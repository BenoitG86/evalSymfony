{% extends 'base.html.twig' %}

{% block title %}PurchaseStatus{% endblock %}

{% block body %}
    <h1>PurchaseStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ purchase_status.id }}</td>
            </tr>
            <tr>
                <th>Initial</th>
                <td>{{ purchase_status.initial ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ purchase_status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_purchase_status_index') }}">back to list</a>

    <a href="{{ path('app_purchase_status_edit', {'id': purchase_status.id}) }}">edit</a>

    {{ include('purchase_status/_delete_form.html.twig') }}
{% endblock %}
